#include <bits/stdc++.h>
using namespace std;

int Subtract(int x, int y)
{
    int Output = x + y;
    return Output;
}

int main()
{
    int x, y;
    cin >> x >> y;
    int result = Subtract(x, y);
    cout << result << endl;
    return 0;
}