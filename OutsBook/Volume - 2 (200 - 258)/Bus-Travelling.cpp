#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, e, f, g;
    cin >> a >> b >> c >> d >> e >> f >> g;
    int avg = (a + b + c + d + e + f + g) / 7;
    cout << avg << endl;
    return 0;
}